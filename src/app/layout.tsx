import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Harihara Gopalan",
  description:
    "As an Associate Professor at Sri Ramakrishna Engineering College, I have been teaching and researching various aspects of cybersecurity, blockchain, networking, and programming for over sixteen years.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
